# Company UI production runtime — install only from the company-approved Python index.
# Keep this file minimal: deployment installs exactly these external runtime libraries.
nicegui==3.15.0
Pillow==12.3.0
python-pptx==1.0.2
