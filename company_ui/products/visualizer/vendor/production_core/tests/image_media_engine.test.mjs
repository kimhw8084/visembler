import assert from 'node:assert/strict';import {prepareImagePlan,assetToView,viewToAsset,sanitizeSvg,ImageMediaError} from '../core/image_media_engine.mjs';
const plan=prepareImagePlan({asset:{id:'8k',width:7680,height:4320,mime:'image/png'},crop:{x:.1,y:.15,width:.7,height:.6},annotations:[{x:.33,y:.44,label:'hotspot'}]});assert.equal(plan.annotations.length,1);let err=0;for(let i=0;i<3000;i++){const p={x:.1+.7*((i%101)/100),y:.15+.6*(((i*17)%101)/100)},v=assetToView(p,plan.crop,{width:840,height:520}),q=viewToAsset(v,plan.crop,{width:840,height:520});err=Math.max(err,Math.abs(p.x-q.x),Math.abs(p.y-q.y))}assert.ok(err<1e-12);const safe=sanitizeSvg(`<svg viewBox="0 0 10 10"><script>alert(1)</script><rect x="1" y="1" width="8" height="8" onclick="x()" fill="#000"/></svg>`);assert.ok(!/script|onclick/i.test(safe));assert.throws(()=>sanitizeSvg('<!DOCTYPE svg [<!ENTITY x SYSTEM "file:///etc/passwd">]><svg/>'),ImageMediaError);console.log(JSON.stringify({pass:true,roundTrips:3000,maxError:err,svgSanitized:true,pixelPolicy:true},null,2));
