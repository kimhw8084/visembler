# Visembler CHG-40 R1 exact-candidate evidence

This is an inspection-only baseline for source `5ee3ffae1d0ed5a4844510a8807c3fef3ea1491f`. The bound worktree started clean; local HEAD, local/remote main, and the bound work branch were verified against that SHA. Product/runtime/release source was not modified. This package is repository-artifact-only and is not a main-integration candidate.

## Locator

`evidence/ui-perfection/CHG-40-r1/` on branch `verify/visembler-chg40-ui-perfection-benchmark-r1`.

## What ran

The maintained native verifier passed its local internal-pilot gates and ran the real native editor, Data First, Chart Studio, report-hub, recovery, worker, operations, and responsive acceptance paths. Diagram Studio was run directly. The final visual cleanup path was also run. Its objective receipts are preserved even where checks failed. The temporary repeat editor benchmark and bounded browser audit are preserved as `.txt` source snapshots under `commands/` together with the exact commands.

## Coverage and interpretation

See `state-matrix.json` for the explicit workflow × state × environment × role × data-shape matrix. Unsupported or not separately executed dimensions are listed rather than synthesized. `manifest.json` records every captured state and SHA-256. `contact-sheet.html` and `contact-sheet.png` index the decisive full-resolution pixels.

`findings.json` reports objective failures and high-confidence friction only. No aesthetic GO is assigned; independent human review is required for hierarchy, salience, grouping, rhythm, typography, density, color semantics, discoverability and craftsmanship.

## Performance

`performance.json` preserves the complete maintained `measure_editor.py` result, raw repeat editor arrays (1/20/100 elements; 2 cold samples, 5 warmups, 7 warm samples), three complete maintained 10/100/1000 capacity repetitions, p50/range summaries, environment and limitations. These are bounded local observations, not universal SLAs. The 1000-report scoped-list measurement is the dominant repository-side cost at roughly 2.68–2.69 s in the repeated sample; the editor warm-ready sample is roughly 404–545 ms.

## Important observed failures

The final visual remediation receipt observed an invalid SVG `viewBox` console error (`VR055`), a process-health/Data First benchmark with zero wafer dies/empty primary evidence (`FV024`/Stage D), and generic promoted WaferFab utilization failures (`VR004`/`FV015`). A bounded computed contrast sample also found below-threshold text. These remain findings; no source was changed to make them pass. Direct data-backed Chart Studio and Diagram Studio ownership checks passed independently.

## Transport and limitations

No qualified external binary-artifact transport was present in the repository-native paths. This bounded package is the fallback transport. Screenshots are lossless PNG copies from maintained native browser runs or the bounded audit; reports retain raw diagnostics and hashes. Browser is Chromium `151.0.7922.34`, NiceGUI `3.15.0`, Playwright `1.62.0`, Pillow `12.3.0`, headless native host. High-contrast and 200% zoom were not synthesized or claimed.
